# Atoll: high-rate ST 2110 multicast receive (L24 audio 1ms ptime, raw video) needs a large
# UDP receive buffer; without this the udpsrc buffer-size request is denied and packets drop.
net.core.rmem_max=33554432
