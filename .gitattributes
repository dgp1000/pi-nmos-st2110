# Shell scripts must stay LF: they run under WSL/bash and are scp'd to the Pi,
# where CRLF line endings break execution. (Repo has core.autocrlf=true.)
*.sh text eol=lf
# Images are binary -- never apply line-ending conversion (core.autocrlf=true would corrupt them).
*.png binary
*.jpg binary
*.gif binary
